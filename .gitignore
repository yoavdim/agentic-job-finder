# ---- interview/application prep (not part of the job-search tracker) ----
application_prep/

# ---- per-job application material (cover letters, etc. — personal) ----
per job material/

# ---- personal info (resume, interview answers, PII) ----
my info/
.kiro/steering/job-search-prefs.md
.kiro/steering/simplify-company-aliases.md

# ---- real working data (kept local only; samples ship separately) ----
# Only shortlist.sample.md / applied.sample.md are committed (fictional demo data).
/shortlist.md
/applied.md
/thoughts.md
/manual.md
/watchlist.md

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
env/

# ---- IDE / editor ----
.idea/
.vscode/
*.swp
*~

# ---- OS ----
.DS_Store
Thumbs.db

# ---- pre-apply snapshots of the (gitignored) data files ----
# The scripts back up shortlist.md/applied.md/etc before every destructive write, since
# git is not an undo for them. Same personal data, so never committed.
.kiro/backups/

# ---- scratch / temp ----
*.tmp
*.log
